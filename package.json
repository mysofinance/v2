{
  "name": "hardhat-project",
  "scripts": {
    "clean": "hardhat clean && rm -rf abi/ && rm -rf artifacts/ && rm -rf dist/ && rm -rf typechain/",
    "compile": "hardhat compile",
    "prettier": "prettier --config .prettierrc --check \"{contracts,test}/**/*.{js,json,sol,ts}\"",
    "prettier:write": "prettier --config .prettierrc --write \"{contracts,test}/**/*.{js,json,sol,ts}\"",
    "test": "npm run test:mainnet && npm run test:arbitrum && npm run test:local",
    "test:arbitrum": "HARDHAT_CHAIN_ID=31336 npx hardhat test ./test/*-arbitrum-*.ts",
    "test:mainnet": "HARDHAT_CHAIN_ID=1 npx hardhat test ./test/*-mainnet-*.ts",
    "test:local": "npx hardhat test ./test/*-local-*.ts",
    "coverage:mainnet": "HARDHAT_CHAIN_ID=1 npx hardhat coverage  --testfiles \"test/*-mainnet-*.ts|test/*-local-*.ts\"",
    "coverage:arbitrum": "HARDHAT_CHAIN_ID=31336 npx hardhat coverage  --testfiles \"test/*-arbitrum-*.ts\"",
    "lint": "solhint \"contracts/**/*.sol\"",
    "slither": "slither . --checklist",
    "prepare": "husky install"
  },
  "devDependencies": {
    "hardhat": "^2.13.0",
    "hardhat-contract-sizer": "^2.8.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "prettier": "^2.8.3",
    "prettier-plugin-solidity": "^1.1.1",
    "solhint": "^3.4.1",
    "solhint-plugin-prettier": "^0.0.5"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@openzeppelin/contracts": "^4.8.0",
    "@openzeppelin/merkle-tree": "^1.0.2",
    "@primitivefi/hardhat-dodoc": "^0.2.3",
    "@uniswap/sdk-core": "^3.2.1",
    "@uniswap/v3-sdk": "^3.9.0",
    "dotenv": "^16.0.3",
    "hardhat-abi-exporter": "^2.10.1",
    "prettier-plugin-solidity": "^1.1.1",
    "solidity-coverage": "^0.8.2"
  }
}
